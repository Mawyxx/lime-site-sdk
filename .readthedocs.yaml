version: 2
build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  commands:
    - pip install -r docs/requirements.txt
    - pip install .
    - mkdocs build --strict
mkdocs:
  configuration: docs/mkdocs.yml
