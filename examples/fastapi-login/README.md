# FastAPI site login (canonical)

```bash
pip install lime-sites-sdk fastapi uvicorn
export LIME_SITE_TOKEN=st_...
# uvicorn main:app — see main.py
```

Agent worker uses `lime-agents-sdk` with the returned `request_id`.
