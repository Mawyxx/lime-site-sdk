# Minimal site ↔ agent loop

Requires `lime-sites-sdk` + `lime-agents-sdk` and both tokens in the environment.

```bash
export LIME_SITE_TOKEN=st_...
export LIME_AGENT_TOKEN=at_...
python main.py
```
